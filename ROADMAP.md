# Roadmap

排序依据是 **价值 / 成本**，不是"想做什么"。工作量是粗估，只用来排序。

欢迎开 Issue 影响这个排序 —— 如果某一项对你很重要，说出来它就会往上走。

---

## 🥇 优先级 1：把"要开多久"这个问题消掉

### 1.1 `lidawake while` —— 绑定进程生命周期

```bash
lidawake while -- make -j            # 编译期间保持唤醒，命令退出自动关
lidawake while -- python train.py    # 训练完自动关
lidawake while --pid 12345           # 盯住一个已经在跑的进程
```

**为什么值得做**：现在用户必须回答"这活儿要跑多久"，而这恰恰是他们不知道的。
定时开太短任务被断，开太长忘了关就耗电。绑定进程把这个问题彻底消除。
对 AI Agent / 构建脚本场景来说，这是最自然的用法。

**实现**：kqueue 的 `EVFILT_PROC` + `NOTE_EXIT` 监听目标 pid 退出 —— 内核级通知，零轮询，
和现有架构（无轮询）一致。守护进程侧新增一种会话模式 `.whilePID(pid_t)`，
决策引擎加一条判定：进程已不存在 → `release(.processExited)`。

**风险**：pid 复用。用 `proc_pidinfo` 记录进程启动时间一并校验。

工作量：小（引擎 + 一种会话模式 + CLI 子命令 + 单测）

### 1.2 守卫触发时发系统通知

现在电量过低 / 过热自动关闭**只写日志**，用户视角就是"任务莫名断了"。这是真实缺口。

**实现**：守护进程不方便发通知（root、无 GUI session），所以走现成的 XPC 反向推送 ——
App 收到状态变化，发现 `lastReleaseReason` 是守卫类原因就发 `UNUserNotification`。
App 没运行时退化为只记日志（可接受）。

工作量：小

---

## 🥈 优先级 2：把电池风险变成可见的数字

项目目前有一个诚实承认的盲区：**验证了"睡没睡"，没有测过功耗**。
与其继续估算，不如让工具自己采集 —— 这是任何文档都给不了的、属于用户自己这台机器的数据。

### 2.1 合盖续航预估

菜单栏直接显示：**「按当前功耗还能合盖跑 ≈ 2h10m」**

**实现**：会话期间按固定间隔采样电量百分比，算下降速率（%/h），结合当前电量算剩余时间。
也可以直接读 `IOPSGetTimeRemainingEstimate()` 作为交叉校验。
采样间隔取现有的 300s 兜底心跳，不新增唤醒。

### 2.2 会话历史与统计

每次会话结束记一条：起止时间、发起方（`origin` 字段已经有了）、结束原因、电量变化、平均 %/h。

攒够几次之后可以回答「我合盖编译一小时大概掉 25%」。
`lidawake history` 输出，菜单里也放一个入口。

**实现**：追加到 `/Library/Application Support/LidAwake/history.jsonl`，滚动保留最近 N 条。
注意：这是行为数据，只存本地、不上传、卸载时随状态目录一起删除。

工作量：中

---

## 🥉 优先级 3：开源基础设施（对采用率影响最大）

坦白说这一档对 star 和实际使用人数的影响，比功能开发更大。

| 项 | 说明 | 工作量 |
|---|---|---|
| **英文 README** | 目前全中文，直接挡掉大部分潜在用户 | 小 |
| **界面 i18n** | 至少 en + zh-Hans，菜单/CLI/报告三处 | 中 |
| **Homebrew Cask** | `brew install --cask lidawake` 一行装完 | 小（但需要先有公证过的包才顺） |
| **GitHub Actions CI** | 跑 85 项单测 + 构建，README 挂绿色 badge。对陌生人是"这项目是认真的"最便宜的信号 | 小 |
| **App 图标** | 目前只有 SF Symbols，没有 `.icns` | 小 |
| **CHANGELOG.md** | 遵循 Keep a Changelog | 小 |

---

## 优先级 4：交互与集成

### 4.1 全局快捷键

用 Carbon 的 `RegisterEventHotKey` —— **不需要辅助功能权限**（这是它相对
`CGEventTap` 的关键优势），所以不会给用户增加授权负担。

### 4.2 Shortcuts.app / AppIntents 支持

macOS 13+ 的 `AppIntents`，让「快捷指令」能开关 LidAwake，可接入更大的自动化流程。
比 CLI 更适合非开发者用户。

### 4.3 会话 Hook（有安全前提）

会话开始 / 结束时执行用户指定脚本，例如结束前 `pkill -TERM myjob` 优雅收尾，或发个消息通知。

**安全约束（不可妥协）**：
- 脚本路径由**用户态 App** 读取并执行，**绝不能**让 root 守护进程执行任意命令
- 否则等于把当前那个"XPC 协议只能表达 off/indefinite/until，不含任何路径与命令"的
  核心安全属性（见 docs/REVIEW.md R2-1）亲手拆掉
- 配置文件放用户目录，权限 0600，路径必须是绝对路径且校验非组/全局可写

工作量：中（大部分成本在把安全边界做对）

---

## 优先级 5：拿到 Developer ID 之后

这两项现在做不了，纯粹因为本机 0 个代码签名身份（见 docs/SPEC.md §1）。
详细步骤和现成代码在 [docs/DISTRIBUTION.md](docs/DISTRIBUTION.md)。

1. **改用 `SMAppService.daemon`**（macOS 13+）替代手写 LaunchDaemon ——
   安装不再需要 `osascript` 提权弹窗，用户在「系统设置 → 登录项」里授权，可随时开关
2. **补上 XPC 客户端签名校验** —— 把"任何 admin 进程都能调用"收紧到"只有我签名的进程能调用"，
   关掉 docs/SPEC.md §7 记录的已知局限

---

## 明确不做

| 想法 | 为什么不做 |
|---|---|
| 保持**屏幕**常亮作为主打功能 | 系统设置里已有，且是安全风险。当前只作为一个默认关闭的选项存在 |
| 外接显示器 / 分辨率管理 | 与"合盖续跑"无关，属于另一个产品 |
| 云同步 / 账号体系 | 一个开关不需要账号 |
| 日历集成（有会议自动开） | 过度设计，猜用户意图容易猜错 |
| 实时 TUI 监控面板 | `lidawake status --json` + 你喜欢的工具已经够了 |
| 为了上 App Store 砍掉守护进程 | 会失去纯电池合盖能力 —— 那是本项目唯一比现成方案强的地方。要上架就单独发 Lite 版，见 docs/DISTRIBUTION.md |
